import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.auraquarium.focus',
  appName: 'Stay Focus - AuraQuarium',
  webDir: 'out'
};

export default config;
